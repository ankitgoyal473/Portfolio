import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Hire Ankit Goyal | AGentX",
  description:
    "Hire Ankit Goyal for production agentic AI systems, RAG pipelines, and MCP development.",
};

export default function HireLayout({ children }: { children: React.ReactNode }) {
  return children;
}
